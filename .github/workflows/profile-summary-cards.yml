name: GitHub-Profile-Summary-Cards

on:
  schedule:
    - cron: "0 0 * * *" # 毎日UTC 0:00に自動更新
  workflow_dispatch: # 手動での実行ボタンを有効化

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.SUMMARY_CARDS_TOKEN || secrets.SUMMARY_GITHUB_TOKEN || github.token }}
        with:
          USERNAME: ${{ vars.SUMMARY_CARDS_USERNAME || github.repository_owner }}
